= `arconia update spring-ai`

The `arconia update spring-ai` command upgrades your project to a newer version of https://spring.io/projects/spring-ai[Spring AI]. It uses upgrade recipes from the https://github.com/arconia-io/arconia-migrations[Arconia Migrations] project, which automatically apply the necessary changes: dependency version bumps, API renames, and configuration property updates required by the new version.

== Usage

Preview what changes will be made without applying them:

[source,shell]
----
arconia update spring-ai --to-version=2.0 --dry-run
----

Apply the update:

[source,shell]
----
arconia update spring-ai --to-version=2.0
----

Pass extra parameters to the underlying build tool using `--`:

[source,shell]
----
arconia update spring-ai --to-version=2.0 -- --stacktrace
arconia update spring-ai --to-version=2.0 -- -DmyProperty=value
----

WARNING: Commit your project to version control before running this command so you can review the changes and revert if needed.

TIP: Consider using `--dry-run` first to inspect what the migration recipe will change before applying it.

== Options

The following options are available:

[cols="1,1,3"]
|===
|Option |Default |Description

|`--dry-run`
|`false`
|Preview the changes that would be made without modifying any files. Recommended before applying the update.

|`--to-version`
|`-`
|Spring AI target version to upgrade to.

|`--verbose` or `-v`
|`false`
|Include verbose output.

|`--help` or `-h`
|
|Display help information for the command.
|===

== Build Tool Integration

The command runs the appropriate OpenRewrite task based on your build tool, following the same approach used by the xref:migration/rewrite-run.adoc[`arconia rewrite run`] command.

Use `--` to pass parameters directly to the underlying build tool. The `--` separator is required to distinguish Arconia CLI options from build tool parameters.

== See Also

* xref:update/spring-boot.adoc[arconia update spring-boot]
* xref:migration/rewrite-run.adoc[arconia rewrite run]
* https://docs.arconia.io/arconia-migrations/latest/[Arconia Migrations Documentation]
* https://spring.io/projects/spring-ai[Spring AI]
